<Window x:Class="PagesRouges.View.EditSiteView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PagesRouges.View"
        mc:Ignorable="d"
        Title="EditSiteView" Height="150" Width="250"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <TextBox Text="{Binding PreviousSiteName, Mode=TwoWay}"
                 Height="20"
                 Grid.Row="0"/>
        <Button Content="Valider"
                Command="{Binding ValidateEditCommand}"
                Grid.Row="1"/>
    </Grid>
</Window>
