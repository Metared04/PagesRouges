<UserControl x:Class="PagesRouges.View.ServicesListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PagesRouges.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="75"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBox Text="Rechercher ..."
                         Width="300"
                         Height="30"
                         Grid.Column="0"/>
                <Button Name="searchNameButton"
                        Content="Chercher"
                        Margin="5,0,0,0"
                        Grid.Column="1"/>
                <TextBlock Text="Service"
                           Grid.Column="2"/>
                <Button Content="+"
                        Command="{Binding AddServiceCommand}"
                        Grid.Column="3"
                        Width="30"/>
            </Grid>
        </StackPanel>
        
        <DataGrid ItemsSource="{Binding CurrentServiceList}" 
                  AutoGenerateColumns="False" 
                  HeadersVisibility="Column" 
                  CanUserAddRows="False" 
                  GridLinesVisibility="Horizontal" 
                  Grid.Row="1"
                  FontFamily="Palatino Linotype"
                  VerticalAlignment="Stretch"
                  HorizontalAlignment="Stretch">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" 
                                    Binding="{Binding ServiceId}" 
                                    Width="*"/>
                <DataGridTextColumn Header="Nom service" 
                                    Binding="{Binding ServiceName}" 
                                    Width="180"/>
                
                <DataGridTemplateColumn Header="Option" 
                                        Width="65">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">                                
                                <Button Content="Supprimer"
                                Command="{Binding DataContext.DeleteServiceCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                CommandParameter="{Binding}" 
                                Width="60"/>
                            </StackPanel>

                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Option" 
                                        Width="65">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Modifier" 
                                        Command="{Binding DataContext.UpdateServiceCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" 
                                        Width="60"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
