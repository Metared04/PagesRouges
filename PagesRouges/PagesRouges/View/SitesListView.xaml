<UserControl x:Class="PagesRouges.View.SitesListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PagesRouges.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="75"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <ComboBox ItemsSource="{Binding SiteIds}" 
                      SelectedValue="{Binding SelectedNewUserSiteId, Mode=TwoWay}"
                      SelectedValuePath="SiteId"
                      DisplayMemberPath="SiteName"
                      Margin="5,0,5,0"
                      Height="20"/>
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <Button Name="searchNameButton"
                    Content="Chercher"
                    Command="{Binding SearchSiteCommand}"
                    Margin="5,0,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <Button Content="Rafraichir"
                    Command="{Binding RefreshSiteListCommand}"/>
                </StackPanel>
                <StackPanel Grid.Column="3">
                    <Button Content="+"
                    Command="{Binding AddSiteCommand}"/>
                </StackPanel>
            </Grid>
        </StackPanel>

        <DataGrid ItemsSource="{Binding CurrentSiteList}" 
                  AutoGenerateColumns="False" 
                  HeadersVisibility="Column" 
                  CanUserAddRows="False" 
                  GridLinesVisibility="Horizontal" 
                  Grid.Row="1"
                  FontFamily="Palatino Linotype"
                  VerticalAlignment="Stretch"
                  HorizontalAlignment="Stretch">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" 
                                    Binding="{Binding SiteId}" 
                                    Width="*"/>
                <DataGridTextColumn Header="Nom site" 
                                    Binding="{Binding SiteName}" 
                                    Width="90"/>

                <DataGridTemplateColumn Header="Option" 
                                        Width="65">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Supprimer"
                                        Command="{Binding DataContext.DeleteSiteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" 
                                        Width="60"/>
                            </StackPanel>

                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Option" 
                                        Width="65">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Modifier" 
                                        Command="{Binding DataContext.UpdateSiteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" 
                                        Width="60"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
