<Window x:Class="PagesRouges.View.UserLoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PagesRouges.View"
        mc:Ignorable="d"
        Title="Connexion" Height="550" Width="800"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        AllowsTransparency="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.Background>
            <SolidColorBrush Color="#C2241D"/>
        </Grid.Background>
        <TextBlock Grid.Row="1" 
                   Foreground="#EBEBE9" 
                   Text="Nom :" 
                   TextAlignment="Center" 
                   FontFamily="Palatino Linotype" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   Padding="35,0,0,0"
                   Margin="20, 0, 20, 0"/>
        <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Row="2" 
                 Margin="150,5,150,10"/>
        <TextBlock Grid.Row="3" 
                   Foreground="#EBEBE9" 
                   Text="Mot de passe :" 
                   TextAlignment="Center" 
                   FontFamily="Palatino Linotype" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   Padding="35,0,0,0"
                   Margin="20, 0, 20, 0"/>
        <!--
        <customcontrols:BindablePasswordBox Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            Grid.Row="4"
                                            Margin="150,5,150,10"/>
        -->
        <TextBlock Grid.Row="6"
                   Text="{Binding ErrorMessage}"
                   Foreground="#EBEBE9"
                   FontWeight="Bold"
                   TextAlignment="Center"
                   Margin="10,0,10,10"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource BooleanToVisibility}}"/>
        <Button Content="Se connecter" 
                Command="{Binding LoginCommand}"
                Background="#FAFA00" 
                Foreground="#000000" 
                Grid.Row="8" 
                Height="30" 
                Margin="0,5,0,10" 
                HorizontalAlignment="Center" 
                Width="418" 
                FontFamily="Palatino Linotype" 
                FontWeight="Bold" 
                FontSize="15"/>
    </Grid>
    
</Window>
